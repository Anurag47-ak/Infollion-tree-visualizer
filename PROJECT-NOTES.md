# Project Notes

- React Flow is used for the tree canvas and interactions.
- Sample data, layout logic, and node behavior are implemented in `src/App.tsx`.
- Verified command: `npm run build`.
- The project is ready to run locally with `npm install` followed by `npm run dev`.
